<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.alarcon.arquetanatureble" >

    <uses-sdk
        android:maxSdkVersion="19"
        android:minSdkVersion="19"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" >
    </uses-feature>

    <application
        android:name=".CORE.BLE_Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service android:name=".CORE.ServiceDetectionTag" />

        <activity
            android:name=".GUI.ScanActivity"
            android:label="@string/app_name"
            android:parentActivityName=".GUI.ScanActivity"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GUI.DeviceActivity"
            android:label="@string/title_activity_sensor"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".GUI.ResumenActivity"
            android:label="@string/title_activity_resumen"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".GUI.ParametersDeviceActivity"
            android:label="@string/title_activity_parameters_device"
            android:screenOrientation="portrait">
        </activity>
        <receiver
            android:name=".BroadCastReceiver.BLEBroadcastReceiver"
            android:enabled="true" >
            <intent-filter>
                <action android:name="es.alarcon.arquetanatureble.DEVICE_FOUND" />
            </intent-filter>
            <intent-filter>
                <action android:name="es.alarcon.arquetanatureble.ADVERTISING_DATA" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".CORE.ServiceDetectionTag$ServiceBroadcastReceiver"
            android:enabled="true" >
            <intent-filter>
                <action android:name="es.alarcon.arquetanatureble.NOTIFY_NEW_DEVICE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
